/**
 * @param {number} target
 * @param {number[]} nums
 * @return {number}
 */
var minSubArrayLen = function(target, nums) {
    let len =nums.length
    let start=0;
    let sum =0
    let min=len;
    for(let end=0;end<len;i++){

    }
};